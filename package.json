{
    "private": true,
    "scripts": {
        "doc": "cargo doc --no-deps --all-features",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "up": "git push && git push --tags --prune",
        "reset": "git reset Head~ --soft",
        "install:rpg": "cargo install",
        "u": "cargo upgrade --workspace",
        "p": "cargo workspace publish --resume"
    }
}
